# API Configuration
# ใช้สำหรับกำหนด URL ของ Backend API
# ใน development mode จะใช้ http://localhost:8000/api โดยอัตโนมัติ
# ใน production ให้ใส่ URL ของ Backend ที่ deploy ไว้ (เช่น Render, Railway, หรือ Vercel)

VITE_API_URL=https://your-backend-api.com/api
